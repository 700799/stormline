// ClickHouse decision/action log — M5 stretch, intentionally empty for now.
// Planned: insert one row per decision and per executed action
// (ts, tick, threatId, tool, assetId, rationale, result_ok) using
// CLICKHOUSE_URL / CLICKHOUSE_PASSWORD.
export {};
