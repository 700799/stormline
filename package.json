{
  "name": "stormline",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous severe-weather operations agent — senses forecasts, decides with Claude on Bedrock, acts through Composio, shows a live dashboard.",
  "type": "module",
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch --env-file=.env src/server.js"
  },
  "dependencies": {
    "@anthropic-ai/bedrock-sdk": "^0.30.2",
    "@composio/core": "^0.10.0",
    "express": "^5.1.0"
  }
}
